name: External Dependency Guard

on:
  pull_request:
  push:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run external dependency guard
        run: bash scripts/check-no-external-tags.sh
